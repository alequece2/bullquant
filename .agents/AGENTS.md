# Regras Globais de Agente

- **Git Push:** NUNCA usar `git push --force` ou `git push -f`. Se houver conflitos, resolvê-los de forma interativa ou pedir orientação ao utilizador. A partir de agora, o workflow é criar branches (ex: `feature/nome-da-feature`), fazer commits, e abrir Pull Requests para análise.
